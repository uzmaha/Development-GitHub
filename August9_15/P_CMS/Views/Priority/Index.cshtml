@model IEnumerable<P_CMS.Models.Priority>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PriorityType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PriorityType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriorityId }) |
          
            @Html.ActionLink("Delete", "Delete", new { id=item.PriorityId })
        </td>
    </tr>
}

</table>
